class rfib{
    static int fib(int n) {
        if (n <= 2)
            return 1;
        else
            return rfib.fib(n-1) + rfib.fib(n-2);
    }

    public static void main() {
        int n, r;

        n = 5;

        r = rfib.fib(n);
    }
}
